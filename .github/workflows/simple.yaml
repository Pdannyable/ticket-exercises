name: shell commands

on: [push]

jobs:
  run-shell-command:
  steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: echo string
      run: echo "Hello Engineers"
    - name: Multiline script
      run:
        node -v
        pwd